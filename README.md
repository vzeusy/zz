# SSHPRO SPACE PROTO v0.5.8.0 — Public Distribution

Distribuição binária para Ubuntu/Debian amd64 e arm64.

O repositório público não contém o código-fonte Go do motor SPACE PROTO/UDPGW.

Instalação:

```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/vzeusy/zz/main/install.sh)"
```

Após instalar:
- `spaceproto` — menu
- `spacectl status` — status

Base: v0.5.8.0 USUARIOS.DB SINGLE SOURCE / MAX SPEED / BHTTP80 / XHTTP443.
